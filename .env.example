# Database - MySQL (XAMPP / phpMyAdmin)
# Copy to .env and set your MySQL user/password. Default XAMPP: user root, no password.
# DATABASE_URL="mysql://USER:PASSWORD@localhost:3306/DATABASE_NAME"
DATABASE_URL="mysql://root:@localhost:3306/prmd"

#!/usr/bin/env

# Database - MySQL (XAMPP / phpMyAdmin)
# Copy to .env and set your MySQL user/password. Default XAMPP: user root, no password.
# DATABASE_URL="mysql://USER:PASSWORD@localhost:3306/DATABASE_NAME"
DATABASE_URL="mysql://root:@localhost:3306/prmd"

# Optional: set for AI-powered chatbot replies (OpenAI)
# OPENAI_API_KEY="sk-..."

# URL of your Python face recognition service.
# Example (Flask/FastAPI running locally): http://127.0.0.1:5000/verify
# The Next.js API route /api/face-verify will forward requests here.
FACE_SERVICE_URL="http://127.0.0.1:5000/verify"
